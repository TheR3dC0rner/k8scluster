apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: kgateway
  namespace: flux-system
spec:
  interval: 10m
  url: oci://cr.kgateway.dev/kgateway-dev/charts/kgateway-crd
  ref:
    tag: "v2.1.1"

